import styled from 'styled-components';
import {TextContainer} from '../Styles'
import { FONT_SIZE, SPACING } from '../../utils/constants'

export const AboutContainer = styled.div`
  display: flex;
  height: 100%;
`;

export const AboutText = styled(TextContainer)`
  margin: auto;
`;

